# AI Agent 全栈工程师训练营课程体系

> 版本：v1.0
>
> 适用对象：有基础开发经验、希望系统转型 AI Agent 工程的后端、全栈和平台工程师。
>
> 学习方式：每一章都遵循“先理解问题 -> 跟做最小实现 -> 加入工程约束 -> 接入前端 -> 写测试和评测 -> 复盘失败”的顺序。

这份文档是 [00.md](./00.md) 的配套课程正文。`00.md` 负责回答“学什么、为什么学、最终交付什么”；本文件负责回答“每一步怎么学、怎么做、怎样判断真的学会了”。

## 课程目录

| 部分 | 内容 | 主要产出 |
|------|------|----------|
| 第一部分 | 全局认识与主线技术栈 | 学习地图和统一事件协议 |
| 第二部分 | 开课前准备 | 可启动的前后端项目 |
| 第三部分 | LLM Gateway | 流式对话页和统一模型服务 |
| 第四部分 | Tool Runtime 与 MCP | 工具治理平台和审计页 |
| 第五部分 | Agent Loop 与 Codebase Agent | 可恢复的代码任务 Agent |
| 第六部分 | Context、Memory 与 RAG | 代码知识页和项目记忆 |
| 第七部分 | Multi-Agent 与 A2A | 协作任务页和委托协议 |
| 第八部分 | Agent Eval | Eval Dashboard 和回归流水线 |
| 第九部分 | 生产工程与 FinOps | 运维治理台和成本报告 |
| 第十部分 | 合规与治理 | 审批、审计和 Kill Switch |
| 第十一部分 | 综合项目 | 可部署的全栈 Agent 产品 |
| 第十二部分 | 持续学习 | 复盘、进阶和长期能力积累 |

---

## 一、先建立全局认识

### 1.1 你最终要做出的东西

整套课程围绕一个持续演进的项目展开：**Agent Engineering Platform**。

它不是九个互不相关的小 Demo，而是同一个平台不断增加能力：

1. 第一章让平台能够稳定调用模型，并通过网页流式返回结果。
2. 第二章让平台能够安全地发现和调用工具。
3. 第三章让平台能够执行多步任务、暂停、恢复和修改代码仓库。
4. 第四章让平台能够检索代码和文档，并管理项目记忆。
5. 第五章让多个 Agent 分工协作。
6. 第六章让你能用数据证明 Agent 是否变好了。
7. 第七章让平台具备部署、监控、成本控制和回滚能力。
8. 第八章让平台具备身份、权限、审计、脱敏和人工监督能力。
9. 第九章把平台包装成一个真正可使用的全栈产品。

每章结束时，项目都应该能运行；下一章不是推倒重来，而是在上一章的代码和数据上增加能力。

### 1.2 主线技术栈

为了让初学者不被过多工具分散注意力，课程使用一条主线技术栈。其他工具只做对比或选修实验。

| 领域 | 主线选择 | 使用目的 |
|------|----------|----------|
| 后端 | Python 3.12、FastAPI、Pydantic v2 | API、数据校验和服务编排 |
| 模型调用 | OpenAI-compatible Provider 抽象 | 统一接入云端或本地模型 |
| 数据库 | PostgreSQL | 用户、任务、工具、评测和审计数据 |
| 缓存/队列 | Redis | 会话、短期状态、事件和限流 |
| 检索 | PostgreSQL + pgvector 或 Qdrant 二选一 | 向量检索和项目知识库 |
| Agent 编排 | 先手写最小 Loop，后使用 LangGraph 对照 | 先理解原理，再学习框架 |
| 前端 | React/Next.js、TypeScript | 对话、任务、审批、Trace 和 Dashboard |
| 前端测试 | Playwright | 验证真实用户流程 |
| 后端测试 | pytest、httpx | 单元测试、接口测试和 Agent 回归测试 |
| 部署 | Docker Compose | 本地和教学环境的一键启动 |

课程不会要求你同时熟练掌握所有主线工具。每次引入一个工具，都先解释它解决什么问题、没有它会怎样、为什么现在引入。

### 1.3 你需要补齐的盲点

很多 Agent 课程默认学员已经懂这些内容，导致初学者在后面反复卡住。本课程明确补齐以下盲点：

- **HTTP 不等于模型调用**：模型调用也是网络请求，仍然会超时、限流、断开和返回非法数据。
- **模型输出不等于程序结果**：自然语言必须经过 Schema 校验，才能进入业务代码。
- **工具调用不是函数调用的语法糖**：工具有权限、风险、审计、超时和副作用。
- **Agent 不是一个更长的 Prompt**：Agent 是模型、工具、状态、上下文和执行环境组成的循环系统。
- **上下文不是越长越好**：无关信息会污染模型决策，也会增加延迟和成本。
- **RAG 不是上传文件后自动变聪明**：解析、切分、召回、重排、引用和评估每一步都可能出错。
- **多 Agent 不等于多调用几次模型**：协作需要任务边界、上下文边界、权限边界和结果协议。
- **Demo 成功不等于系统可靠**：必须保存任务集、轨迹、失败样本和版本对比结果。
- **部署成功不等于上线**：上线还需要密钥、租户、审计、告警、成本、灰度和回滚。
- **前端不是最后加一层页面**：Agent 的暂停、审批、失败和恢复都必须在 UI 中可理解、可操作。

### 1.4 每次学习的固定流程

每个小节都按下面的顺序进行。不要跳过“失败实验”，因为 Agent 工程能力主要来自定位失败。

1. 阅读本节的“要解决的问题”。
2. 用最小代码实现成功路径。
3. 主动制造一次失败：超时、非法 JSON、错误参数、权限不足或模型拒答。
4. 为失败增加日志、错误类型和可恢复策略。
5. 接入前端，让用户能看见过程和状态。
6. 写测试或 Golden Task。
7. 写一段复盘：失败发生在哪一步，证据是什么，修复后指标是否变化。

---

## 二、开课前准备

### 2.1 前置能力自测

如果下面的任务完全不会，先花 1-3 天补齐，不要直接进入 Agent Loop：

- 用 Python 写一个函数，并用 pytest 测试它；
- 用 FastAPI 创建 `GET /health` 和 `POST /echo`；
- 用 `httpx.AsyncClient` 调用一个 HTTP 接口；
- 用 SQL 创建一张表并完成增删改查；
- 用 Git 创建分支、提交代码和查看 diff；
- 用 React/TypeScript 创建输入框、按钮和列表；
- 用 `fetch` 接收普通 JSON 响应；
- 用 Docker 启动一个 Redis 或 PostgreSQL 容器。

### 2.2 环境准备

建议环境：macOS、Linux 或 Windows WSL2。Windows 原生环境遇到 Docker、Shell 和文件权限问题时，优先使用 WSL2。

```bash
python3 --version
node --version
docker --version
git --version
```

建议版本：Python 3.12、Node.js 20 LTS、Docker Compose v2。版本不完全一致也可以学习，但遇到依赖错误时必须先确认版本，而不是反复重装。

创建课程项目：

```bash
mkdir agent-platform
cd agent-platform
python3 -m venv .venv
source .venv/bin/activate
python -m pip install --upgrade pip
pip install fastapi uvicorn pydantic httpx pytest pytest-asyncio
npm create next-app@latest web -- --ts --eslint --app
```

课程推荐的初始目录：

```text
agent-platform/
├── apps/
│   ├── api/                 # FastAPI 服务
│   └── web/                 # Next.js 前端
├── packages/
│   ├── contracts/           # 前后端共享事件和数据协议
│   └── evals/               # Golden Tasks 和评测脚本
├── infra/                   # Docker Compose、数据库初始化
├── docs/                    # 架构图、复盘和 Runbook
└── README.md
```

如果你还不会组织 Python 包，可以先把代码放在 `apps/api` 中；课程后面会再解释为什么要拆分 `packages`。

### 2.3 第一个健康检查

在写 Agent 之前，先让后端和前端分别能启动。后端至少提供：

```python
from fastapi import FastAPI

app = FastAPI(title="Agent Platform")

@app.get("/health")
async def health() -> dict[str, str]:
    return {"status": "ok"}
```

运行并确认浏览器打开 `/docs`，再启动 Next.js 页面。若这一步失败，先修复环境，不要把问题误判为模型问题。

### 2.4 课程统一事件格式

从第一章开始，前后端不直接约定“返回一段字符串”，而是使用 Agent Event。后面所有流式、工具、审批、Trace 和恢复都基于它扩展。

```json
{
  "event": "message.delta",
  "run_id": "run_001",
  "sequence": 3,
  "data": {"text": "你好"},
  "created_at": "2026-07-30T00:00:00Z"
}
```

第一阶段只需要 `run.started`、`message.delta`、`run.completed`、`run.failed`；后续逐步增加 `tool.started`、`approval.required`、`state.changed` 和 `trace.span`。

---

## 三、第一章：LLM API、Prompt 与结构化输出

### 3.1 本章要解决的问题

学完本章，你应该能回答：

- 模型请求到底发送了什么数据？
- `temperature`、Token 和 Context Window 对结果有什么影响？
- 为什么模型说“看起来像 JSON”仍然不能直接交给程序？
- 为什么流式输出要用事件协议，而不是不断拼接字符串？
- 多家模型供应商的差异应该放在哪里隔离？

本章不做复杂 Agent，只做一个稳定的 LLM Gateway 和一个能正常工作的流式对话页。

### 3.2 先补基础：模型、Token 和上下文

#### 模型是什么

把模型先理解成一个“根据输入预测下一段输出”的远程函数。它可以帮助你生成答案，但不会天然保证事实正确、格式正确或执行成功。

一次调用至少包含：

- 模型名称；
- 消息列表或输入文本；
- 系统指令；
- 用户输入；
- 可选的工具定义或结构化 Schema；
- 采样参数；
- 超时和取消信号。

#### Token 是什么

Token 是模型处理文本的计费和计算单位，不等于字数。中文、英文代码和符号的 Token 比例不同。工程上关心三个结果：

1. 输入 Token 越多，成本和延迟通常越高；
2. 输入加输出超过上下文窗口，请求可能失败或被截断；
3. 历史消息、工具 Schema、工具结果都会占用上下文预算。

#### 模型参数怎么理解

- `temperature`：输出随机性，不是“智能程度”；
- `top_p`：候选概率截断，初学阶段不要同时调它和 temperature；
- `max_tokens`：输出上限，不保证模型一定用完；
- `timeout`：网络等待上限，不能用来控制模型思考质量；
- `seed`：部分模型支持的复现辅助参数，不代表绝对确定性。

### 3.3 第一个模型调用：先写最小版本

先写一个只负责调用 Provider 的函数，不要第一天就引入 Agent 框架。

```python
from typing import Any
import httpx

async def complete(
    *,
    base_url: str,
    api_key: str,
    model: str,
    messages: list[dict[str, str]],
) -> dict[str, Any]:
    async with httpx.AsyncClient(timeout=30) as client:
        response = await client.post(
            f"{base_url}/chat/completions",
            headers={"Authorization": f"Bearer {api_key}"},
            json={"model": model, "messages": messages},
        )
        response.raise_for_status()
        return response.json()
```

手动检查四种情况：

1. 正常返回；
2. API Key 缺失；
3. 服务商返回 401、429 或 500；
4. 网络超过 30 秒没有响应。

你要理解：`raise_for_status()` 只能把 HTTP 错误抛出来，不能帮你判断业务响应是否完整。还要检查响应中是否存在候选结果、消息内容和用量信息。

### 3.4 Provider 抽象和模型能力矩阵

不要在业务代码里到处写供应商特有字段。定义统一接口：

```python
from dataclasses import dataclass
from typing import Protocol

@dataclass
class ModelResponse:
    text: str
    input_tokens: int | None
    output_tokens: int | None
    model: str

class ModelProvider(Protocol):
    async def complete(self, messages: list[dict[str, str]]) -> ModelResponse:
        ...
```

然后建立能力矩阵，至少记录：

| 能力 | 为什么记录 |
|------|------------|
| Context Window | 判断长文本是否能放入请求 |
| Structured Output | 判断能否直接使用 Schema |
| Tool Calling | 判断能否进入第二章 |
| Vision | 判断能否处理图片 |
| 输入/输出价格 | 成本估算和路由 |
| 平均延迟 | 用户体验和超时策略 |

不要把“模型更强”写成抽象结论。把模型能力写进配置，后面用评测数据决定路由。

### 3.5 Prompt Engineering：从能回答到可复用

Prompt 至少拆成四层：

1. **角色**：模型在系统中承担什么职责；
2. **任务**：这一次具体要完成什么；
3. **约束**：不能做什么、必须遵守什么；
4. **输出协议**：必须返回什么结构。

一个适合初学者的模板：

```text
你是项目文档助手。

任务：根据用户提供的代码摘要，说明模块职责和调用关系。

约束：
- 不要编造没有出现在输入中的文件和函数。
- 不确定时明确写“未确认”。
- 每个结论都要引用输入中的证据。

输出：使用 Markdown，包含“结论、证据、未确认事项”三个小节。
```

需要特别说明：Chain-of-Thought 不应被当作必须暴露给用户的答案格式。工程上更重要的是让模型返回可验证的计划、工具参数、证据和结论。

### 3.6 Structured Output：让模型返回程序能处理的数据

自然语言不能直接作为业务协议。先定义 Pydantic 模型：

```python
from pydantic import BaseModel, Field

class TaskSummary(BaseModel):
    title: str = Field(min_length=1)
    status: str
    evidence: list[str] = Field(default_factory=list)
    confidence: float = Field(ge=0, le=1)
```

学习顺序：

1. 先让模型返回普通 JSON；
2. 用 `TaskSummary.model_validate()` 校验；
3. 故意提交缺字段、错类型和越界数字；
4. 为校验失败定义重试次数；
5. 重试仍失败时返回明确的降级结果，而不是把半成品交给下游。

错误处理至少分三类：

- **可重试**：临时限流、网络断开、模型返回格式错误；
- **不可重试**：API Key 无效、模型不存在、请求权限不足；
- **需要人工处理**：多次输出不满足业务协议、内容涉及高风险决策。

### 3.7 Streaming：前后端共同完成的功能

流式输出不是把完整答案拆成很多次普通响应，而是服务端保持连接并不断发送事件。后端要处理：

- 连接建立；
- 增量内容；
- Provider 错误；
- 用户取消；
- 连接断开后的状态保存；
- 最终完成或失败事件。

前端要处理：

- 新事件追加到当前消息；
- `run.failed` 显示可理解的错误；
- 用户点击停止后取消请求；
- 刷新页面后通过 `run_id` 查询历史状态。

建议先实现自定义的四类事件，再学习 SSE；不要直接把 Provider 的原始响应透传给浏览器。

### 3.8 第一章前端交付与实验步骤

按顺序完成，不要一次性复制完整项目：

1. 创建 `/health` 和 `/v1/chat`；
2. 将 API Key 从代码移到环境变量；
3. 实现 Provider 接口和统一响应模型；
4. 添加超时、重试和错误映射；
5. 添加结构化输出参数；
6. 添加 SSE 事件转换；
7. 记录 `run_id`、模型、输入 Token、输出 Token、延迟和错误；
8. 创建 Next.js 对话页；
9. 支持停止生成和重新加载历史任务；
10. 为成功、失败、取消和结构化输出失败各写一个测试。

### 3.9 第一章常见盲点

- **把 API Key 放前端**：任何浏览器代码都可能被用户看到，密钥必须只存在后端。
- **把所有错误都重试**：401、权限错误和非法请求重试没有意义。
- **只记录最终答案**：没有原始输入、模型版本和 Prompt 版本，后面无法复盘。
- **把流式文本拼接后再解析 JSON**：结构化输出应在协议层单独处理，不能依赖字符串截取。
- **没有取消机制**：用户已经离开页面，服务仍继续消耗 Token。
- **前端只做 loading**：Agent 任务会暂停、失败、等待审批，必须使用明确状态。

### 3.10 第一章验收

提交以下内容：

- 多模型统一调用服务；
- 流式对话页；
- 结构化输出示例；
- 成本和延迟日志；
- 4 类异常的测试；
- 10 个 Golden Tasks；
- 一页失败复盘。

通过标准：正常任务可以完成；模型、网络和输出格式失败时，用户能看到明确状态；任何 API Key 都不会出现在前端或日志中。

### 3.11 与下一章的衔接

本章的 Gateway 只会调用模型。下一章要解决的问题是：模型如何请求外部能力，以及系统如何判断这个请求是否允许执行。你会把本章的统一响应、Trace 和事件协议继续用于 Tool Runtime。

---

## 四、第二章：Function Calling、Tool Runtime 与 MCP

### 4.1 本章要解决的问题

学完本章，你应该能回答：

- 模型为什么不能直接执行 Python 函数？
- 工具的输入、输出和错误为什么都要有 Schema？
- 谁决定一个工具能不能被某个 Agent 使用？
- 工具超时、重复执行和写操作风险怎么处理？
- MCP 和普通 HTTP API 分别解决什么问题？

本章完成一个 Tool Runtime：模型只负责提出工具调用意图，Runtime 负责校验、授权、执行、审计和返回结果。

### 4.2 先理解 Function Calling 的真实流程

一次工具调用不是“模型直接调用函数”，而是四步：

1. 应用把工具名称、描述和输入 Schema 一起发送给模型；
2. 模型返回一个工具调用请求和参数；
3. 应用验证参数并判断权限；
4. 应用执行工具，再把结果作为新消息送回模型。

模型可以选择错误的工具、生成错误参数、重复调用或在不该调用时调用。因此不能把模型输出当作授权。

### 4.3 工具协议设计

每个工具至少有以下字段：

```python
from typing import Literal
from pydantic import BaseModel

class ToolSpec(BaseModel):
    name: str
    description: str
    input_schema: dict
    output_schema: dict
    risk_level: Literal["read", "write", "high"]
    required_scopes: list[str] = []
    timeout_seconds: int = 20
    max_retries: int = 1
    version: str = "1.0.0"
```

为什么要分别定义 Input、Output 和 Error：

- 输入决定参数是否合法；
- 输出决定下游如何处理；
- 错误决定 Agent 是重试、改参数、换工具还是请求人工介入。

### 4.4 Tool Registry：工具注册和发现

先用内存字典实现，再迁移到 PostgreSQL。注册中心负责：

- 注册和下线工具；
- 查询工具元数据；
- 按任务或权限筛选工具；
- 处理工具版本；
- 禁止同名工具覆盖；
- 保存工具依赖和负责人。

初版 API：

```text
POST /tools
GET  /tools
GET  /tools/{name}
POST /tools/{name}/disable
POST /tools/{name}/invoke
GET  /tool-calls/{run_id}
```

练习：注册 `get_weather`、`search_code`、`read_file` 三个只读工具，再注册一个 `write_file` 写工具。让前端能显示它们的风险等级。

### 4.5 参数校验和执行管道

工具执行必须经过固定管道：

```text
收到 Tool Call
  -> 解析 JSON
  -> 校验 Input Schema
  -> 检查用户身份和 Scope
  -> 检查风险等级
  -> 必要时请求人工确认
  -> 设置超时和资源限制
  -> 执行工具
  -> 校验 Output/Error Schema
  -> 写入审计和 Trace
  -> 返回结构化结果
```

任何一步失败，都必须有明确的 `ToolError` 类型。不要让工具内部的 Python 异常直接泄漏到模型上下文。

建议的错误类型：

- `InvalidArguments`：参数缺失或类型错误；
- `PermissionDenied`：身份没有权限；
- `ApprovalRequired`：需要用户确认；
- `Timeout`：超过执行时间；
- `ExternalServiceError`：下游服务失败；
- `ToolDisabled`：工具已被下线；
- `OutputValidationError`：工具返回不符合协议。

### 4.6 权限、风险和人工确认

把工具按副作用分级：

| 等级 | 示例 | 默认策略 |
|------|------|----------|
| read | 搜索、读取文件、查询天气 | 可自动执行，但要记录 |
| write | 写文件、创建工单、修改数据库 | 需要更严格 Scope，教学阶段建议确认 |
| high | 删除数据、执行 Shell、发送外部请求 | 默认禁止，必须人工确认和审计 |

注意：RBAC 只回答“这个用户属于什么角色”，还要检查当前任务、租户、资源和工具风险。权限判断应由 Runtime 完成，不应让模型自己决定“我有权限”。

人工确认事件至少包含：

- 谁发起；
- 将要执行什么动作；
- 影响哪些资源；
- 使用哪些参数；
- 允许、拒绝还是过期；
- 用户确认时间和审计 ID。

### 4.7 MCP：协议边界要讲清楚

MCP 主要解决 Agent 如何标准化发现和使用外部 Tools、Resources、Prompts。它不是权限系统，也不是 Agent Loop，也不自动保证工具安全。

记住这三个关系：

- Function Calling：模型如何提出工具调用；
- Tool Runtime：应用如何治理和执行工具；
- MCP：外部工具和资源如何用统一方式接入。

先实现一个本地 MCP Server，提供 `list_files` 和 `read_file`，再写 MCP Client 接入 Tool Runtime。每一步都保留原始请求、响应、权限判断和错误。

### 4.8 第二章前端实验

实现“工具管理与审计页”：

1. 工具列表显示名称、版本、风险等级和启用状态；
2. 点击工具显示 Input Schema 和所需权限；
3. 使用测试参数调用只读工具；
4. 写工具调用时弹出确认面板；
5. 调用结束显示参数、结果、耗时、重试和 Trace ID；
6. 权限拒绝、超时和工具下线显示不同的错误状态。

前端不要显示“模型正在思考”这种无法验证的状态。显示可以被后端事件证明的状态，例如“已提交工具调用”“等待确认”“工具执行失败”。

### 4.9 第二章实验步骤

1. 定义 `ToolSpec`、`ToolResult` 和 `ToolError`；
2. 实现内存版 Registry；
3. 实现三个只读工具和一个写工具；
4. 加入 Pydantic 参数校验；
5. 加入 Scope 和 Risk 检查；
6. 加入超时、重试和审计记录；
7. 实现模型工具调用循环；
8. 实现本地 MCP Server 和 Client；
9. 接入工具管理与审计页；
10. 主动测试越权、错误参数、工具超时和重复调用。

### 4.10 第二章常见盲点

- **工具描述写得越长越好**：描述应清楚说明用途、参数、限制和副作用，过长会浪费上下文。
- **只校验 JSON 格式不校验业务规则**：日期范围、资源归属和允许操作仍需业务校验。
- **重试写操作**：网络超时不代表写操作没有成功，重试前必须考虑幂等键。
- **把 MCP Server 当成可信程序**：外部 Server 仍需白名单、权限、版本和审计。
- **把工具结果原样塞给模型**：结果可能过长、包含敏感数据或包含恶意指令，需要清洗和截断。

### 4.11 第二章验收

提交：

- Tool Registry 和 Tool Runtime；
- 至少四个工具和一个 MCP Server；
- 权限矩阵和风险分级表；
- 工具管理/审计前端；
- 至少 8 个工具调用测试；
- 一次写操作审批和一次越权拦截的 Trace。

通过标准：模型不能绕过 Runtime 直接执行工具；每次调用可追踪；失败不会让 Agent 无限重试；高风险操作不会默认执行。

### 4.12 与下一章的衔接

现在系统可以安全执行一次或几次工具调用，但还不能管理长任务。下一章会把一次调用扩展为 Loop，并增加任务状态、Checkpoint、Sandbox 和代码仓库操作。

---

## 五、第三章：Agent Loop、State Machine 与 Codebase Agent

### 5.1 本章要解决的问题

学完本章，你应该能回答：

- Agent Loop 每一轮到底做什么？
- 什么时候继续，什么时候停止？
- 任务暂停后，恢复需要保存哪些信息？
- 为什么运行 Shell 和修改代码必须放进 Sandbox？
- 如何让 Agent 在代码仓库中完成“理解 -> 修改 -> 测试 -> 修复”？

本章是从“调用模型”进入“构建 Agent 系统”的分水岭。

### 5.2 最小 Agent Loop

先实现一个可以解释的 Loop：

```text
接收任务
  -> 读取当前状态
  -> 构造上下文
  -> 调用模型
  -> 如果返回最终答案：结束
  -> 如果返回 Tool Call：校验并执行
  -> 保存工具结果和状态
  -> 检查预算、循环次数和停止条件
  -> 进入下一轮
```

伪代码：

```python
async def run_agent(task: str, max_steps: int = 12):
    state = create_state(task)
    for step in range(max_steps):
        decision = await model.decide(build_context(state))
        if decision.kind == "final":
            state.complete(decision.answer)
            return state
        if decision.kind == "tool_call":
            result = await runtime.invoke(decision.tool, decision.arguments)
            state.record_tool_result(result)
            continue
        state.fail("unsupported decision")
        return state
    state.fail("max_steps_exceeded")
    return state
```

这段代码故意不复杂。先把每一轮的输入、模型决策、工具结果和状态变化打印出来，再考虑框架化。

### 5.3 停止条件和死循环

至少设置：

- 最大步数；
- 最大 Token 预算；
- 最大运行时间；
- 重复工具调用次数；
- 没有新信息的重复计划；
- 用户取消；
- 高风险操作等待人工确认。

死循环的典型表现：反复搜索同一个文件、不断重新规划、工具报错后用同样参数重试。检测死循环不能只看次数，还要比较最近几轮的工具名、参数和结果摘要。

### 5.4 State Machine 和 Checkpoint

任务状态和模型输出不是一回事。定义明确的状态：

```text
created
  -> planning
  -> running
  -> waiting_approval
  -> running
  -> completed
  -> failed / cancelled
```

Checkpoint 至少保存：

- 任务目标和用户身份；
- 当前状态和当前步骤；
- 已执行的工具、参数、结果摘要和 Trace ID；
- 当前计划和已完成子目标；
- 重试次数和预算消耗；
- 等待中的审批；
- 可恢复所需的版本信息。

不要把所有历史原文都塞进 Checkpoint。原始 Trace 放在日志或对象存储，状态表保存恢复所需的最小信息。

### 5.5 Sandbox 和执行边界

Codebase Agent 需要读写文件、运行测试和执行命令，这些动作可能影响宿主机。初学阶段先理解三层边界：

1. **应用层边界**：允许访问哪些路径和命令；
2. **进程层边界**：超时、内存、CPU 和子进程限制；
3. **容器层边界**：文件系统、网络、用户权限和密钥隔离。

教学版可以先实现受限本地 Sandbox：

- 只允许访问课程临时目录；
- 使用命令白名单；
- 禁止删除根目录和未知路径；
- 默认关闭外网；
- 每个命令设置超时；
- 保存 stdout、stderr、退出码和耗时。

Docker Sandbox、Firecracker 和 E2B 可以作为架构对比，不要在没有理解本地边界时直接依赖它们。

### 5.6 Codebase Agent 的工具集合

第一版只实现这些工具：

| 工具 | 作用 | 默认权限 |
|------|------|----------|
| `list_files` | 查看目录 | read |
| `search_code` | 搜索文本或符号 | read |
| `read_file` | 读取指定文件 | read |
| `read_directory` | 获取目录摘要 | read |
| `write_file` | 写入文件 | write + approval |
| `apply_patch` | 应用小范围修改 | write + approval |
| `run_test` | 执行固定测试命令 | read/execute |
| `git_diff` | 查看变更 | read |
| `git_status` | 查看仓库状态 | read |

暂时不要让模型自由拼接 Shell。把高频安全操作封装成工具，才能限制参数、记录审计并在前端展示。

### 5.7 从代码理解到自动修复

使用三个任务逐级练习：

**任务一：代码理解**

“找出登录逻辑并生成说明。”

要求 Agent 输出文件路径、函数名、调用关系和证据，不允许只给总结。

**任务二：测试生成**

“为指定函数补充测试。”

要求先读取已有测试和项目运行命令，生成测试后必须运行并记录结果。

**任务三：失败修复**

“根据测试失败日志修复问题。”

要求每次修改前说明假设，修改后重新运行测试，最后输出 diff、测试结果和未解决风险。

### 5.8 前端：任务工作台

实现一个以任务为中心的页面，而不是只有聊天框：

- 左侧显示任务目标和当前状态；
- 中间显示步骤、工具调用和增量输出；
- 工具写操作显示审批卡片；
- 失败状态显示原因、重试和取消按钮；
- 完成后显示修改文件、测试结果和 Trace ID；
- 刷新页面后根据 `run_id` 恢复任务。

前端状态必须来自后端事件。不要根据“等待了 3 秒”猜测 Agent 是否完成。

### 5.9 AI Coding Agent 实战边界

Claude Code、Cursor 等工具作为真实开发工具案例学习，重点是通用方法：

- 如何写项目约定文件；
- 如何限制上下文和工具权限；
- 如何使用 Git Worktree 隔离任务；
- 如何让 Agent 先读、再计划、再改、再测；
- 如何审查 Agent 生成的 diff。

不要把某个产品当前版本的按钮位置当成核心知识。产品界面会变，任务分解、权限控制、验证和复盘不会变。

### 5.10 第三章实验步骤

1. 为任务定义状态模型；
2. 实现不带框架的最小 Loop；
3. 加入最大步数、超时和取消；
4. 加入 Checkpoint 表和恢复接口；
5. 实现安全文件工具；
6. 实现固定测试命令工具；
7. 接入一个示例代码仓库；
8. 完成代码理解任务；
9. 完成测试生成任务；
10. 完成一次失败修复并保存完整 Trace；
11. 用 LangGraph 重写其中一个流程，比较自建 Loop 和框架的差异；
12. 接入任务工作台并完成 Playwright 流程测试。

### 5.11 第三章常见盲点

- **把 Planner 当作必需组件**：简单任务直接 Loop 更清楚，复杂任务才需要计划。
- **状态只保存最后答案**：恢复需要工具结果、步骤和审批状态。
- **Checkpoint 保存 Python 对象**：应保存可序列化、可迁移的业务数据。
- **测试失败就让模型无限修复**：必须有预算、次数和人工接管。
- **把 Sandbox 当成安全保证**：Sandbox 只是边界的一部分，权限、审计和网络策略仍然需要。
- **自动修改用户仓库**：默认先生成 diff，经过人工确认后再应用。

### 5.12 第三章验收

提交：

- Agent Loop 和状态机；
- Checkpoint 与断点续跑；
- 文件、搜索、测试和 Git 工具；
- 本地 Sandbox 设计说明；
- 任务工作台；
- 三个代码仓库任务的 Trace 和复盘。

通过标准：任务可以完成、暂停、取消和恢复；写操作不会无确认执行；测试失败不会无限循环；用户可以从前端理解 Agent 做了什么。

### 5.13 与下一章的衔接

现在 Agent 能执行任务，但它能看到的信息仍然有限。下一章要解决“哪些信息应该放进上下文、哪些信息应该检索、哪些信息应该长期保存”。

---

## 六、第四章：Context Engineering、Memory 与 Codebase RAG

### 6.1 本章要解决的问题

第三章的 Agent 已经会执行任务，但“模型看到了什么”仍然由一段不断变长的消息列表决定。本章要让你能回答：

- 为什么上下文越长不一定越好？
- 哪些信息应该进入模型上下文，哪些信息应该留在数据库？
- 长文件、工具结果和历史对话如何压缩？
- RAG 的召回、重排和引用分别解决什么问题？
- 代码仓库为什么不能只按固定字符数切块？
- 项目记忆和用户长期记忆有什么区别？

本章最终为 Codebase Agent 增加检索、记忆、引用和上下文调试能力。

### 6.2 Context Engineering 基础

一个 Agent 请求的上下文通常包括：

```text
System Instructions
+ 工具定义
+ 用户目标
+ 当前任务状态
+ 最近工具结果
+ 历史对话
+ 检索到的证据
+ 输出格式约束
```

这些内容共同竞争 Context Window。上下文管理的目标不是把更多文字塞进去，而是让当前决策所需的信息优先进入。

建立一个 Context Budget 表：

| 区域 | 内容 | 处理策略 |
|------|------|----------|
| 固定约束 | 系统规则、权限、输出协议 | 版本化，不能随意截断 |
| 当前目标 | 用户任务和验收条件 | 每轮保留 |
| 状态摘要 | 已完成步骤、待解决问题 | 超长时压缩 |
| 工具结果 | 搜索、文件和测试结果 | 保留结论和关键证据 |
| 历史消息 | 旧对话和旧计划 | 按相关性筛选 |
| 检索证据 | 文档、代码和引用 | 限数量，保留来源 |

初学者常犯的错误是把所有历史都放进 Prompt。先记录每一块的 Token 数量，再做压缩，才能知道优化是否有效。

### 6.3 Context Packing 和 Compression

实现一个 `ContextBuilder`，输入任务状态和候选资料，输出最终消息。至少有四个步骤：

1. 删除与当前任务无关的历史；
2. 把完整工具结果转换为摘要和引用；
3. 对长文件进行结构化截断；
4. 在达到预算前停止添加低优先级内容。

优先级可以先使用简单规则：

- 当前用户目标最高；
- 当前失败原因高于旧成功结果；
- 直接引用证据高于模型生成的摘要；
- 当前文件和相关调用链高于整个仓库；
- 最近一次状态高于很早的对话。

不要一开始就使用复杂的语义排序。先做一个能解释的规则版，再用评测数据判断是否值得增加模型排序。

### 6.4 Working Memory、State 和长期记忆

三者要分开：

- **Working Memory**：当前任务临时需要的信息，如目标、已完成步骤、未解决问题。
- **Agent State**：系统恢复任务所需的结构化状态，如当前节点、审批状态和重试次数。
- **Long-term Memory**：跨任务保存的稳定信息，如用户偏好或项目约定。

代码项目还需要 **Project Memory**：

- 技术栈；
- 项目目录结构；
- 安装、构建和测试命令；
- 编码约定；
- 常见故障；
- 最近修复记录；
- 部署方式和环境限制。

记忆写入不能完全由模型自由决定。至少定义：

- 什么内容值得记忆；
- 谁允许写入；
- 记忆的来源和时间；
- 如何修改和删除；
- 记忆冲突时哪个版本优先；
- 用户如何查看和撤销记忆。

### 6.5 RAG 的完整数据流

RAG 不是一个向量数据库调用，而是一个流水线：

```text
文档发现
  -> 解析
  -> 清洗
  -> 元数据提取
  -> 切块
  -> Embedding
  -> 写入索引
  -> Query Rewrite
  -> 召回
  -> 过滤
  -> Rerank
  -> 上下文压缩
  -> 引用生成
  -> 答案与证据一致性检查
```

每一步都要记录版本。切块策略变了、Embedding 模型变了、Rerank 参数变了，都可能导致结果变化。

### 6.6 文档解析和 Chunking

针对不同内容选择不同切块策略：

| 内容 | 初始策略 | 主要风险 |
|------|----------|----------|
| Markdown | 按标题和段落 | 标题层级丢失 |
| HTML | 按正文结构清洗 | 导航、广告污染 |
| PDF | 按页面/段落解析 | 表格和阅读顺序错误 |
| 表格 | 保留行列关系 | 单行切块失去表头 |
| Python/TypeScript | 按函数、类和文件 | 依赖关系被切断 |
| 日志 | 按事件或时间窗口 | 单行没有完整上下文 |

每个 Chunk 应保存：

- 文档 ID 和版本；
- 文件路径或 URL；
- 页码、行号或标题；
- 上下级标题；
- 创建和更新时间；
- 权限范围；
- 文本内容和向量。

先实现固定大小和标题切块的对照实验，再尝试语义切块或 AST 切块。没有基线就无法证明复杂策略更好。

### 6.7 召回、Hybrid Search 和 Rerank

- **向量召回**擅长语义相似，但可能漏掉精确标识符；
- **关键词召回**擅长函数名、错误码和专有名词；
- **Hybrid Search** 合并两种结果；
- **Rerank** 使用更强的模型重新判断候选与问题的相关性。

代码检索建议同时使用：

1. 文件名和符号关键词；
2. 向量相似度；
3. 路径、语言和模块过滤；
4. 结果重排；
5. 同一文件相邻片段合并。

不要把 Top-K 当成固定真理。K 太小会漏证据，K 太大又会污染上下文。使用 Golden Questions 测量召回率和引用准确率。

### 6.8 Codebase RAG 实验

使用一个小型开源仓库，完成以下任务：

- “鉴权链路经过哪些文件？”
- “新增一个支付渠道需要修改哪些模块？”
- “这个异常的根因是什么？请给出代码行号。”
- “项目的测试和部署命令是什么？”

依次比较：

1. 只用文件名搜索；
2. 只用向量检索；
3. 关键词 + 向量 Hybrid Search；
4. Hybrid Search + Rerank；
5. 加入 Project Memory 和引用约束。

每种方案都记录：是否找到关键文件、引用是否支持结论、输入 Token、延迟和最终任务成功率。

### 6.9 前端：代码知识页

代码知识页至少包含：

- 搜索框和任务问题；
- 检索结果列表；
- 文件路径、行号、代码片段；
- 引用被答案使用的位置；
- 当前项目记忆；
- “加入上下文”和“排除结果”操作；
- 检索耗时、召回数量和 Rerank 版本。

用户必须能区分“模型的解释”和“原始代码证据”。引用不可点击或引用错误，是 RAG 产品中非常严重的信任问题。

### 6.10 第四章常见盲点

- **Embedding 相似不等于答案相关**：相似文本可能没有回答问题。
- **只看最终答案评估 RAG**：必须单独评估召回、引用和答案一致性。
- **切块越大越好**：大块提高上下文完整性，但会增加噪声和成本。
- **把所有文档公开给所有租户**：检索前就应该做权限过滤，而不是答案生成后再补救。
- **记忆只增不删**：过期和错误记忆会持续污染后续任务。
- **代码只按字符切块**：代码结构、符号和调用关系比字符长度更重要。

### 6.11 第四章验收

提交：

- ContextBuilder 和 Token Budget 报告；
- Working Memory、Project Memory 和恢复摘要；
- 文档/代码索引流水线；
- Hybrid Search + Rerank；
- 引用和行号展示；
- 五组 RAG 对照实验；
- 代码知识页和失败复盘。

通过标准：Agent 能在上下文受限时继续任务；检索结果带可验证来源；项目记忆可以查看、更新和删除；不同检索方案有数据对比。

### 6.12 与下一章的衔接

现在一个 Agent 能看到更好的信息，但复杂任务仍由一个 Agent 独自完成。下一章会学习什么时候需要多个角色、如何委托子任务，以及如何避免多个 Agent 互相污染。

---

## 七、第五章：多 Agent 协作与 A2A 协议

### 7.1 本章要解决的问题

- 什么任务适合单 Agent，什么任务适合多 Agent？
- 子 Agent 应该拿到哪些上下文和工具？
- 多个结果冲突时由谁裁决？
- 并行执行如何控制成本、超时和失败？
- MCP 和 A2A 的边界是什么？

先完成单组织内的 Supervisor + Specialist 协作，再把 A2A 作为协议扩展。不要一开始就做跨组织系统。

### 7.2 什么时候不要用 Multi-Agent

单 Agent 更适合：

- 目标明确；
- 步骤较短；
- 上下文集中；
- 工具链简单；
- 结果容易验证。

多 Agent 更适合：

- 任务需要多个相互独立的专业视角；
- 子任务可以并行；
- 每个角色需要不同工具或权限；
- 结果需要对照、审查或聚合。

如果只是把一次模型调用拆成三次，并没有清晰的职责和验证标准，多 Agent 往往只会增加成本和不确定性。

### 7.3 三种基础模式

#### Supervisor

主 Agent 负责拆解、调度、收集结果和最终决策。适合代码审查、研究和多角度诊断。

#### Planner-Executor

Planner 生成计划，Executor 执行步骤。适合步骤依赖清晰、需要长程执行的任务。

#### Reviewer

一个 Agent 生成结果，另一个 Agent 依据 Rubric 审查。审查不能只问“你觉得答案好吗”，必须传入具体标准和证据要求。

### 7.4 Subagent 委托协议

每个子任务至少定义：

```json
{
  "goal": "分析订单模块的边界和依赖",
  "context": ["src/order", "tests/order"],
  "allowed_tools": ["list_files", "search_code", "read_file"],
  "scopes": ["repo:read"],
  "output_schema": {
    "findings": "array",
    "evidence": "array",
    "risks": "array"
  },
  "timeout_seconds": 120,
  "max_tool_calls": 20
}
```

“分析项目”不是合格的子任务目标；“分析订单模块的边界，输出三个发现，每个发现附文件和行号”才是可验收的委托。

### 7.5 隔离、并行和结果聚合

子 Agent 之间至少隔离：

- 上下文；
- 工具集合；
- 身份和权限；
- Token 预算；
- 文件写权限；
- 运行时间。

并行执行前确认任务没有写冲突或顺序依赖。结果聚合要保留：

- 原始结论；
- 证据；
- 来源 Agent；
- 置信度或风险等级；
- 冲突点；
- 主 Agent 的裁决理由。

不能只把几个自然语言答案拼接起来。聚合结果必须有统一 Schema。

### 7.6 Skill 系统

Skill 是可复用的任务方法，不只是一个 Prompt 文件。一个 Skill 至少包含：

- 名称和适用场景；
- 输入参数；
- 执行步骤；
- 工具依赖；
- 权限边界；
- 输出协议；
- 质量标准；
- 版本和负责人。

例如“代码审查 Skill”应说明检查安全、测试、性能和可维护性的顺序，而不是只写“请审查代码”。

### 7.7 A2A 和 MCP 的边界

- MCP 主要处理 Agent 与外部工具、资源和 Prompt 的标准化接入；
- A2A 主要处理不同 Agent 之间的能力发现、任务委托和结果通信；
- Tool Runtime 负责本地应用的权限、审计和执行治理；
- A2A 不会自动解决身份、信任、费用和数据权限问题。

A2A 选修实验的最小范围：

1. 定义 Agent Card，声明能力和端点；
2. 定义 Task 的创建、执行、完成和失败状态；
3. 用 SSE 发送状态更新；
4. 做身份校验和任务超时；
5. 记录委托和结果回传的审计。

不要把当前协议版本、厂商支持情况和产品界面当成永恒知识，开课时应根据官方文档核对。

### 7.8 前端：协作任务页

页面需要让用户看懂“谁在做什么”：

- 主 Agent 和子 Agent 的关系图；
- 每个子任务目标、状态、预算和耗时；
- 已完成结果和证据；
- 冲突结果并排显示；
- 主 Agent 的聚合结论和裁决理由；
- 取消某个子任务或转人工的操作。

不要把每个 Agent 的完整思考过程直接展示给用户。展示状态、计划摘要、工具事件、证据和结果即可。

### 7.9 第五章实验步骤

1. 先用单 Agent 完成一次代码审查；
2. 拆出 Architecture、Test、Security 三个只读子任务；
3. 为每个子任务定义输入、工具、权限和输出 Schema；
4. 实现 Supervisor 的串行调度；
5. 确认没有依赖后改为并行；
6. 加入超时、预算和取消；
7. 实现结果去重、排序和冲突标记；
8. 加入 Reviewer Agent 和人工确认；
9. 接入协作任务页；
10. 选修：实现最小 A2A Agent Card 和 Task 生命周期。

### 7.10 第五章常见盲点

- **Agent 越多越高级**：角色增加会增加调用、上下文和冲突成本。
- **主 Agent 把整个上下文传给每个子 Agent**：这会造成成本高、权限宽和上下文污染。
- **并行任务共享可写目录**：会产生不可预测的文件冲突。
- **用 LLM 解决所有冲突**：先用证据、优先级和确定性规则，无法判断再请求人工。
- **子 Agent 没有独立验收标准**：主 Agent 无法判断结果是否完成。
- **把 A2A 当成安全方案**：协议描述通信，不替代 IAM、审计和数据治理。

### 7.11 第五章验收

提交：

- 一个 Supervisor 协作系统；
- 至少三个有不同工具和权限的 Specialist；
- 子任务委托协议；
- 并行、超时、取消和预算控制；
- 结果聚合和冲突处理；
- 协作任务页；
- A2A 选修实验或协议边界报告。

通过标准：每个子任务都有明确目标和证据；权限不会因为委托而扩大；失败的子任务能被识别和重试；最终结果能追溯到子任务证据。

### 7.12 与下一章的衔接

多 Agent 系统能运行不代表它可靠。下一章会建立 Golden Dataset、自动评测、Trace Analysis 和回归测试，让每次修改都有数据依据。

---

## 八、第六章：Agent Eval 工程

### 8.1 本章要解决的问题

- 什么叫“任务成功”？
- 如何评估工具是否选对、参数是否正确？
- LLM-as-Judge 什么时候有用，什么时候会误判？
- Prompt、模型或 RAG 修改后如何发现退化？
- 如何把评测接进 CI/CD 和生产监控？

Eval 不是最后写一张报表，而是从第一章开始积累的工程资产。本章把它变成可以独立运行的平台。

### 8.2 Golden Dataset

一个 Golden Task 至少包含：

```json
{
  "id": "code-understanding-001",
  "category": "codebase",
  "input": "说明登录逻辑并给出代码证据",
  "repo_snapshot": "commit-sha",
  "expected_properties": [
    "包含认证入口",
    "引用真实文件和行号",
    "不编造不存在的函数"
  ],
  "risk_level": "read",
  "max_cost": 0.05,
  "max_latency_seconds": 60
}
```

任务集要覆盖成功、边界和失败：

- 正常代码理解；
- 找不到信息时明确说明；
- 工具参数错误；
- 权限不足；
- 测试失败后的修复；
- 长上下文和上下文压缩；
- 人工审批和取消；
- 多 Agent 结果冲突。

不要只保存“标准答案”。Agent 任务很多没有唯一答案，更适合保存必须满足的属性、证据和禁忌行为。

### 8.3 指标体系

按层次记录指标：

| 层次 | 指标 | 说明 |
|------|------|------|
| 任务 | Task Success Rate | 任务是否完成并满足验收条件 |
| 工具 | Tool Selection Accuracy | 是否选对工具 |
| 工具 | Parameter Accuracy | 参数是否正确且合法 |
| 检索 | Retrieval Recall | 关键证据是否被召回 |
| 答案 | Citation Accuracy | 引用是否支持结论 |
| 稳定性 | Failure Recovery Rate | 失败后是否恢复 |
| 体验 | P95 Latency | 大多数用户的等待时间 |
| 成本 | Cost per Run | 单次任务成本 |
| 安全 | High-risk Block Rate | 高风险动作拦截比例 |

指标必须写清分母和判定规则。比如“成功率 80%”要说明是 10 个任务成功 8 个，还是 1000 个任务中 800 个通过。

### 8.4 规则评测、Schema 评测和人工评测

- **规则评测**：状态是否完成、工具是否在白名单、是否包含必需字段；稳定但覆盖有限。
- **Schema 评测**：输出能否通过结构校验；适合工具参数和结构化答案。
- **Snapshot Test**：对固定输入比较关键输出；容易受模型措辞变化影响。
- **Golden Answer**：适合有相对稳定答案的任务；不适合开放式研究。
- **Human Eval**：适合质量、风格和复杂业务判断；成本高但可校准自动评测。
- **LLM-as-Judge**：适合按 Rubric 评估开放答案；必须做偏差检测和人工抽样。

不要用一个 Judge 分数覆盖全部质量。至少同时保存规则结果、证据、Trace 和人工抽样结果。

### 8.5 LLM-as-Judge 的正确使用方式

Judge 输入应包含：

- 任务目标；
- Agent 最终答案；
- 必要的工具结果和证据；
- Rubric；
- 输出 JSON Schema。

Rubric 示例：

```text
请按以下标准给出 0-2 分：
1. 结论是否回答了任务目标；
2. 每个关键结论是否有证据；
3. 是否包含输入中不存在的事实；
4. 是否遵守了权限和输出格式。

必须返回：总分、每项分数、证据、扣分原因。
```

校准方法：

1. 人工标注一小组样本；
2. 运行 Judge；
3. 比较两者分歧；
4. 分析 Judge 偏好和漏判；
5. 修改 Rubric；
6. 定期抽样复核。

### 8.6 Trace Analysis：从分数追到失败步骤

失败任务必须能回答：

- 模型第一次做错是在什么时候？
- 是 Prompt、上下文、检索、工具、权限还是执行环境的问题？
- 错误是偶发还是重复模式？
- 修改哪个组件最可能改善结果？

Trace 建议包含这些 Span：

```text
run
├── context.build
├── model.call
├── tool.select
├── tool.validate
├── tool.execute
├── state.change
├── approval.wait
└── final.answer
```

每个 Span 保存开始/结束时间、输入摘要、版本、状态和错误，但敏感内容必须脱敏。分析页面要支持按失败类型、工具、模型、Prompt 版本和任务类别筛选。

### 8.7 回归测试和版本对比

每次修改以下内容都要跑评测：

- System Prompt；
- 工具 Schema 和描述；
- 模型或模型参数；
- RAG 切块、Embedding 和 Rerank；
- Agent Loop 终止条件；
- 权限和审批策略；
- 前端事件协议。

比较至少四项：成功率、成本、P95 延迟和安全拦截率。一个版本成功率提高但成本翻倍，不一定是更好的版本。

### 8.8 Eval Pipeline

最小流水线：

```text
提交 Prompt/代码变更
  -> 构建固定环境
  -> 运行 Golden Tasks
  -> 保存 Trace 和指标
  -> 与基线比较
  -> 发现退化则阻止合并
  -> 生成评测报告
```

先用命令行实现，再接入 CI。不要一开始就构建复杂 Dashboard，而不先保证评测结果可重复和可保存。

### 8.9 前端：Eval Dashboard

至少包含：

- 任务集和任务分类；
- 版本选择和对比；
- 成功率、成本、延迟和安全指标；
- 失败样本列表；
- 单个任务的完整 Trace；
- Judge 评分和人工复核结果；
- “回归失败”明确标记和原因。

Dashboard 的颜色不能替代数字和判定规则。所有图表都要能追溯到具体任务和版本。

### 8.10 第六章实验步骤

1. 从前三章任务中选出 20 个 Golden Tasks；
2. 为每个任务定义成功属性和失败属性；
3. 实现规则评测和 Schema 评测；
4. 保存每次运行的 Trace 和指标；
5. 加入 LLM-as-Judge 和人工抽样；
6. 统计 Prompt、工具和模型版本的差异；
7. 实现失败类型分类；
8. 完成一次 Prompt 回归；
9. 接入 CI/CD；
10. 实现 Eval Dashboard；
11. 写一份“改进前后对比”报告。

### 8.11 第六章常见盲点

- **只评估最终文本**：Agent 成功还包括工具、权限、引用、成本和恢复。
- **只评估成功案例**：没有失败样本就无法发现边界。
- **Judge 分数当真理**：Judge 也需要校准和人工抽样。
- **基线不固定**：环境、仓库、模型版本变化后，前后对比没有意义。
- **指标越多越好**：指标必须服务于决策，避免做无法解释的仪表盘。
- **评测和生产分离**：生产失败 Trace 应该回流为新的 Golden Task。

### 8.12 第六章验收

提交：

- 至少 20 个 Golden Tasks；
- 任务集管理和版本字段；
- 规则、Schema、Judge 三类评测；
- Trace Analysis；
- 回归测试和 CI Pipeline；
- Eval Dashboard；
- 一份包含退化样本和修复过程的报告。

通过标准：你能用数据说明一个改动带来了什么影响；失败能定位到执行步骤；评测结果可重复；生产失败能够进入后续任务集。

### 8.13 与下一章的衔接

现在平台知道质量如何变化，但还需要在真实服务中控制延迟、成本、版本、发布和故障。下一章进入生产工程与 FinOps。

---

## 九、第七章：生产工程与 FinOps

### 9.1 本章要解决的问题

- 本地能运行的 Agent 如何部署成稳定服务？
- API Key、数据库、Redis 和向量库如何配置和隔离？
- 如何知道一次任务花了多少钱、慢在哪里、失败在哪里？
- Prompt、模型、工具和 RAG 版本如何灰度和回滚？
- 如何在成本下降时避免质量一起下降？

本章先完成 Docker Compose 和可观测性，再做成本策略。vLLM、TGI、Triton 和 Kubernetes 属于进阶选修，不作为主线部署的前置条件。

### 9.2 开发环境和生产环境的差异

开发环境可以容忍手动操作，生产环境必须考虑：

- 服务进程是否会重启；
- 数据库是否持久化；
- 多个请求是否并发；
- 密钥是否泄漏；
- 版本是否可回滚；
- 失败是否有告警；
- 用户是否能看到其他租户的数据。

不要把“在我的电脑上能运行”当成部署完成。部署验收至少需要新机器或干净环境重新启动。

### 9.3 Docker Compose 部署

第一版服务包含：

```text
web       Next.js 前端
api       FastAPI 后端
postgres  任务、用户、工具、评测和审计数据
redis     缓存、事件和短期状态
vector    向量检索服务，可选 pgvector 或 Qdrant
```

每个服务明确：

- 镜像和版本；
- 环境变量；
- 数据卷；
- 健康检查；
- 依赖关系；
- 日志输出；
- 资源限制。

健康检查不等于进程存在。API 的 `/health` 还应该检查数据库和 Redis 是否可连接，但不要在每个请求中重复做昂贵的深度检查。

### 9.4 配置、密钥和租户隔离

配置分三类：

- **代码配置**：默认超时、事件名称和功能开关；
- **环境配置**：数据库 URL、Provider 地址和日志级别；
- **密钥**：API Key、数据库密码和签名密钥。

密钥不得写入 Git、前端 Bundle、Trace 原文或异常堆栈。日志脱敏至少处理 API Key、手机号、邮箱、身份证号、合同金额和用户输入中的敏感字段。

租户隔离需要同时覆盖：

- 数据库查询条件；
- 缓存 Key；
- 向量检索过滤；
- 文件路径；
- Trace 查询；
- 成本报表；
- 前端路由和权限。

只在前端隐藏按钮不能形成隔离，后端每次读取和写入都必须检查租户。

### 9.5 可观测性：日志、Trace 和 Metrics

#### 日志

结构化日志至少包含：

- 时间；
- 服务和环境；
- `request_id`、`run_id`、`trace_id`；
- 用户和租户的脱敏标识；
- 事件类型；
- 状态和错误类型；
- 延迟和重试次数。

#### Trace

Trace 贯穿：

```text
用户请求
 -> Context Build
 -> Model Call
 -> Tool Call
 -> Approval
 -> State Change
 -> Subagent
 -> Final Answer
```

每个 Span 都要记录组件版本，才能复现“当时为什么这样做”。

#### Metrics

基础指标：

- 请求数、成功数和失败数；
- P50、P95、P99 延迟；
- Token 输入/输出；
- 工具调用次数和失败率；
- 审批等待时长；
- 任务恢复成功率；
- 每租户、每任务类型和每模型成本。

### 9.6 Replay：把失败任务重新跑一遍

Replay 需要保存足以复现问题的输入，但不能无限保存敏感原文。建议保存：

- 脱敏后的任务输入；
- Prompt 和模型版本；
- 工具 Schema 和版本；
- Context 摘要和检索结果 ID；
- 工具调用参数和结果摘要；
- 状态转移；
- 失败位置。

重放时明确两种模式：

- **真实重放**：重新调用外部服务，可能产生副作用；
- **模拟重放**：使用历史工具结果，只验证 Agent 决策。

默认使用模拟重放，写操作需要额外授权。

### 9.7 FinOps：成本不是一个日志字段

成本治理要回答四个问题：

1. 钱花在了哪个租户、任务和版本？
2. 哪一步消耗了最多 Token 或时间？
3. 降低成本是否影响成功率和安全性？
4. 预算超限时系统如何降级？

为每个 Agent Run 设置预算：

- 最大输入 Token；
- 最大输出 Token；
- 最大工具调用次数；
- 最大运行时间；
- 最大金额；
- 子 Agent 预算。

预算超限可以按顺序处理：压缩上下文、减少检索数量、切换较低成本模型、暂停等待人工或直接终止。降级必须产生事件，不能静默改变质量。

### 9.8 模型路由和缓存

模型路由先用规则实现：

- 简单分类和格式化使用低成本模型；
- 复杂规划和代码修改使用能力更高的模型；
- 失败重试可以切换 Provider；
- 高风险任务不因为便宜而绕过安全检查。

缓存类型：

- Prompt Cache：固定前缀或系统指令；
- Embedding Cache：相同文本不重复向量化；
- Tool Result Cache：只读、可接受短时过期的结果；
- Semantic Cache：相似问题复用答案，但必须考虑权限和时效。

任何缓存都要定义命中条件、租户范围、过期时间和失效策略。用户权限变化后，旧缓存可能必须立即失效。

### 9.9 版本、灰度和回滚

需要版本化的对象：

- 模型和参数；
- System Prompt；
- Tool Schema 和实现；
- Skill；
- RAG 切块、Embedding、Rerank；
- Workflow；
- 前端事件协议；
- 数据库迁移。

灰度发布时先选择一小部分用户或任务类型，观察成功率、成本、P95、错误和安全指标。回滚不仅是服务镜像回滚，还可能需要一起回滚 Prompt、工具和知识库版本。

### 9.10 前端：运维治理台

页面至少包含：

- 服务健康和版本；
- 任务成功率、延迟和成本趋势；
- Trace 搜索；
- Replay 入口；
- 模型/Prompt/工具版本；
- 告警列表；
- 租户和任务类型成本归因；
- 灰度比例和回滚按钮。

回滚按钮必须显示影响范围和确认步骤，不能做成一个点击后立即改变全部流量的危险操作。

### 9.11 第七章实验步骤

1. 编写 Docker Compose；
2. 为 API、数据库和 Redis 加健康检查；
3. 将所有配置移到环境变量；
4. 实现结构化日志和 Trace；
5. 保存模拟 Replay 数据；
6. 统计 Token、延迟和工具成本；
7. 实现 Run、租户和任务类型成本归因；
8. 加入预算护栏和降级；
9. 实现模型路由和缓存对照实验；
10. 模拟一次灰度和回滚；
11. 完成运维治理台；
12. 在干净环境启动并提交部署 Runbook。

### 9.12 第七章常见盲点

- **日志越详细越好**：完整 Prompt 可能包含隐私和密钥，日志需要分层、脱敏和采样。
- **成本只看模型价格**：工具、检索、重试、人工等待和基础设施也有成本。
- **缓存只看命中率**：错误缓存、权限串租户和过期数据比低命中率更危险。
- **只灰度服务代码**：Prompt、模型、工具和知识库也会改变行为。
- **Kubernetes 能解决所有问题**：它解决部署编排，不解决 Agent 质量、权限和成本策略。
- **Replay 直接执行写操作**：默认使用模拟结果，避免回放产生真实副作用。

### 9.13 第七章验收

提交：

- Docker Compose 和干净环境启动记录；
- 结构化日志、Trace、Replay；
- 成本归因和预算策略；
- 模型路由、缓存和降级实验；
- 灰度/回滚演练；
- 运维治理台；
- 上线 Runbook。

通过标准：部署可重复；一次任务能追到模型、工具、状态和成本；预算超限有明确处理；版本变更可以灰度和回滚。

### 9.14 与下一章的衔接

生产系统不只需要可观测，还要回答“谁可以让 Agent 做什么、数据能不能被看到、什么动作必须有人批准”。下一章进入合规与治理。

---

## 十、第八章：合规与治理

### 10.1 本章要解决的问题

- Agent 的身份和用户身份有什么区别？
- 如何做到最小权限和身份传递？
- 审计日志应该记录什么？
- 如何处理敏感数据、租户隔离、数据保留和删除？
- 哪些任务必须人工审批？
- Governance Agent 能做什么，不能替代什么？

本章重点是工程控制。法规条款和时间表必须在开课时以官方资料核对，不把未经确认的日期当成系统设计依据。

### 10.2 风险分级和场景边界

先判断产品是否真的适合使用 Agent：

- 信息检索、文档分析、代码理解、客服辅助通常适合；
- 规则明确、完全确定、脚本即可完成的任务不一定需要 Agent；
- 资金、医疗、法律和招聘等高风险决策必须有人类监督，不能因为模型“看起来很确定”就自动执行。

风险评估至少记录：

- 任务影响范围；
- 错误概率和最坏后果；
- 数据敏感等级；
- 工具副作用；
- 是否可以撤销；
- 是否需要人工批准；
- 失败后如何停止和恢复。

### 10.3 Agent Identity 和权限传递

系统中至少有三种身份：

- 用户身份：谁发起了任务；
- Agent 身份：哪个 Agent 角色在执行；
- 工具/服务身份：最终访问了哪个资源。

调用链必须保留身份关系：

```text
用户 Alice
  -> Supervisor Agent
    -> Security Agent
      -> read_repository Tool
```

子 Agent 不能因为主 Agent 有权限就自动拥有所有权限。委托时明确传入允许的 Scope，并在工具 Runtime 重新检查。

### 10.4 数据治理

核心控制：

- 敏感字段识别、脱敏和最小化采集；
- 租户、部门和项目隔离；
- 向量库权限过滤；
- Trace 和日志的保留期限；
- 用户删除数据后的级联清理；
- 外部 Provider 的数据处理边界；
- API Key 和密钥管理。

脱敏不是简单把手机号替换成星号。需要明确哪些字段必须可追踪、哪些字段可以哈希、哪些字段必须完全删除，以及恢复权限归谁。

### 10.5 审计日志

一条高风险审计至少包含：

- 请求者；
- Agent 和工具；
- 时间；
- 目标资源；
- 输入参数摘要；
- 权限判断；
- 人工审批人和决定；
- 执行结果；
- 失败原因；
- Trace ID 和版本。

审计日志和普通调试日志分开。调试日志可以采样和删除，审计记录需要更严格的完整性、访问控制和保留策略。

### 10.6 Human-in-the-loop 和 Kill Switch

人工审批适合：

- 写文件、删除数据、发送外部请求；
- 资金、医疗、法律和招聘相关动作；
- Agent 预算或权限即将超限；
- 模型输出无法通过规则校验；
- 多个 Agent 结论发生高风险冲突。

审批页面显示：动作、参数、影响、证据、风险等级、过期时间和拒绝原因。审批不是一句“是否继续”，而是让人能做出有依据的决定。

Kill Switch 至少支持：

- 停止单个 Run；
- 禁用某个工具；
- 停止某个 Agent 角色；
- 停止某个租户的自动执行；
- 全局进入只读模式。

Kill Switch 自己也需要权限、审计和恢复说明。

### 10.7 Governance Agent 的正确定位

治理 Agent 可以辅助：

- 汇总审计日志；
- 发现异常行为模式；
- 检查文档是否缺少风险说明；
- 对合规证据做初步分类。

它不能替代：

- 确定性权限拦截；
- 关键数据脱敏；
- 人工审批；
- Kill Switch；
- 法律和合规责任主体。

先实现规则引擎和人工流程，再做 Governance Agent 选修实验。不要让一个不稳定的 Agent 单独监管另一个 Agent。

### 10.8 前端：合规与审计页面

实现以下页面或模块：

- 风险任务列表；
- 审批队列；
- 审计事件详情；
- 工具权限矩阵；
- 敏感字段脱敏预览；
- 租户和数据范围；
- Kill Switch 操作；
- 合规检查清单和缺失项。

用户要能知道“为什么被拦截”和“谁批准了什么”，而不是只看到一个红色错误。

### 10.9 第八章实验步骤

1. 为现有工具建立风险矩阵；
2. 定义用户、Agent 和工具身份；
3. 实现 Scope 检查和身份传递；
4. 为高风险工具增加审批；
5. 实现敏感字段脱敏；
6. 增加租户过滤和向量库权限过滤；
7. 实现审计查询；
8. 实现单 Run、单工具和全局 Kill Switch；
9. 完成合规与审计页面；
10. 选修：用 Governance Agent 做审计异常摘要；
11. 编写一份风险评估和控制映射文档。

### 10.10 第八章常见盲点

- **登录了就有权限**：认证回答“你是谁”，授权回答“你能做什么”。
- **前端隐藏按钮就是安全**：所有权限必须在后端重新检查。
- **审计日志等于普通日志**：审计要关注完整性、可查询和责任链。
- **脱敏只做展示层**：数据进入模型、日志、向量库和缓存前都要考虑。
- **人工审批阻塞任务后没有恢复设计**：审批状态必须可持久化、过期和取消。
- **合规章节只背法规名称**：工程师需要把要求映射为权限、数据、审计和流程控制。

### 10.11 第八章验收

提交：

- 风险矩阵和工具权限矩阵；
- 身份传递设计；
- 脱敏、租户隔离和数据保留方案；
- 审计日志和查询页面；
- 人工审批流程；
- Kill Switch 演练；
- 风险评估文档；
- Governance Agent 选修报告或原型。

通过标准：高风险动作默认不能自动执行；租户数据不会串；审计能回答谁在什么时候让哪个 Agent 做了什么；系统可以被及时停止。

### 10.12 与下一章的衔接

前八章已经形成一个具备模型、工具、长任务、知识、协作、评测、生产和治理能力的平台。最后一章不再增加无边界技术，而是选择一个真实场景，将这些能力组合成可使用、可展示、可答辩的产品。

---

## 十一、第九章：企业级综合项目与产品化落地

### 11.1 综合项目的选择

从下面三个方向选择一个主场景，不要三个一起做：

#### 方向 A：企业级深度研究平台

适合练习多源检索、证据链、报告生成、引用和人工复核。

典型流程：

```text
研究问题
 -> 拆解子问题
 -> 搜索和读取资料
 -> 交叉验证
 -> 保留引用
 -> 生成结构化报告
 -> 人工审阅
 -> 发布和归档
```

#### 方向 B：新一代软件工厂

适合练习需求分析、架构设计、代码修改、测试、审查、Git 和 CI。

典型流程：

```text
需求
 -> 任务拆解
 -> 架构方案
 -> 代码修改
 -> 测试执行
 -> 代码审查
 -> 人工批准
 -> PR/部署
```

#### 方向 C：企业知识与流程工作台

适合练习 RAG、项目记忆、工具调用、审批、权限和成本治理。

典型流程：

```text
用户问题
 -> 权限过滤
 -> 检索知识
 -> 调用业务工具
 -> 生成带引用答案
 -> 需要时发起审批
 -> 保存任务和反馈
```

### 11.2 产品需求先于技术实现

综合项目先写一页产品需求：

- 目标用户是谁；
- 用户现在的痛点是什么；
- 哪些步骤由 Agent 完成；
- 哪些步骤必须人工完成；
- 失败时用户如何处理；
- 成功用什么指标衡量；
- 哪些数据不能被模型看到；
- 第一版明确不做什么。

如果你无法写清楚用户和任务，就不要先选框架。Agent 不是为了展示技术名词，而是为了完成一个有价值的任务。

### 11.3 综合项目最小架构

```text
Next.js 工作台
  -> API Gateway
    -> Auth/Tenant
    -> Agent Run Service
      -> Context Manager
      -> Tool Runtime
      -> State/Checkpoint
      -> Eval/Trace
    -> PostgreSQL / Redis / Vector Store
    -> MCP Server 或业务 API
```

前端至少包含：

- 登录或开发环境身份切换；
- 任务创建和历史；
- 流式输出；
- 工具和审批事件；
- 引用或证据；
- Trace 或 Eval 结果；
- 错误、取消和恢复。

后端至少包含：

- 用户和租户边界；
- Agent Run 和状态持久化；
- Tool Runtime；
- 权限与人工审批；
- Trace、成本和审计；
- Golden Tasks；
- Docker Compose 部署。

### 11.4 十六周综合项目安排

前十四周已经完成平台模块，最后两周按下面推进：

**第十五周：实现和验证**

1. 冻结产品需求和范围；
2. 画架构图和任务状态图；
3. 选择 5 个核心用户任务；
4. 完成主流程；
5. 接入前端工作台；
6. 加入权限、审批、错误和恢复；
7. 跑 Golden Tasks 并修复最高优先级失败。

**第十六周：上线和表达**

1. Docker Compose 干净环境部署；
2. 完成 Trace、成本和审计检查；
3. 做一次灰度或版本切换演练；
4. 完成演示视频；
5. 编写 README、Runbook 和架构说明；
6. 准备 10 分钟技术答辩；
7. 用数据说明一个优化前后的差异。

### 11.5 综合项目验收清单

#### 功能

- 至少一个完整业务流程；
- 至少三类工具；
- 至少一个需要审批的动作；
- 支持失败、取消和恢复；
- 前端能展示事件和结果。

#### 工程

- 输入输出有 Schema；
- 工具调用有权限和审计；
- 任务有状态和 Checkpoint；
- 关键路径有测试；
- 有 20 个左右 Golden Tasks；
- 有成本、延迟和成功率数据。

#### 生产

- Docker Compose 可启动；
- 密钥不入库；
- 日志脱敏；
- 有健康检查和告警说明；
- 有 Replay 或失败复现方法；
- 有回滚和 Kill Switch 说明。

#### 产品

- 用户知道当前任务状态；
- 用户能理解 Agent 的证据和限制；
- 高风险操作不会静默执行；
- 错误信息能指导下一步；
- 页面不是只展示 Demo 文案，而是真正支持操作。

### 11.6 作品集和简历表达

不要只写“使用 LangGraph 构建 Agent”。使用以下结构表达：

```text
场景：为代码仓库提供可恢复的测试修复任务
问题：长任务容易超时，模型容易重复调用工具
方案：状态机 + Checkpoint + Tool Runtime + Sandbox
工程控制：权限分级、人工审批、Trace、Replay、预算护栏
结果：任务成功率从 X 提升到 Y，P95 延迟为 Z，单次成本为 C
证据：Golden Tasks、失败样本、架构图和演示地址
```

面试时重点讲：

- 为什么选 Agent 而不是普通 Workflow；
- 失败最严重的一次是什么；
- 如何定位和修复；
- 如何证明改动有效；
- 如果上线，最担心什么风险。

### 11.7 第九章常见盲点

- **综合项目继续堆功能**：应冻结范围，优先保证主流程可靠。
- **只展示成功演示**：必须主动演示失败、审批、取消和恢复。
- **没有用户和业务指标**：技术指标不能替代任务完成率和人工接管率。
- **前端只是聊天框**：全栈作品需要任务、状态、证据和治理界面。
- **作品集没有可复现说明**：别人无法运行就无法验证工程能力。

### 11.8 第九章验收与答辩

综合项目评分：

| 项目 | 占比 | 重点 |
|------|------|------|
| 主流程完成度 | 25% | 真实任务是否完成 |
| 工程可靠性 | 25% | 状态、恢复、权限、错误和测试 |
| Eval 证据 | 20% | 指标、失败样本和版本对比 |
| 全栈体验 | 15% | 前端状态、审批、证据和可操作性 |
| 部署与表达 | 15% | Runbook、演示和技术答辩 |

答辩必须现场展示一次正常流程和一次失败流程，并回答“如果模型、工具或外部服务失败，你的系统会怎样”。

---

## 十二、持续学习与课程复盘机制

### 12.1 每周固定节奏

建议每周分成四次学习：

1. **理解课**：阅读概念、画数据流和状态图；
2. **跟做课**：完成最小实现；
3. **失败课**：主动制造异常并修复；
4. **交付课**：写测试、评测、文档和复盘。

每次学习都留下提交记录或变更记录。不要只保存最终能运行的代码。

### 12.2 每章复盘模板

```markdown
# 第 X 章复盘

## 我解决了什么问题

## 我现在能解释的核心概念

## 我主动制造过的失败

## 失败发生在哪个边界

## 修复前后的指标

## 仍然不确定的地方

## 下一章会依赖哪些成果
```

### 12.3 遇到问题时的排查顺序

遇到“Agent 不工作”时，不要直接修改 Prompt，按顺序排查：

1. 环境和依赖是否正确；
2. HTTP 请求和认证是否正常；
3. 输入和输出 Schema 是否通过；
4. 工具是否注册、授权和可执行；
5. Context 是否缺少关键证据或包含污染信息；
6. State 和 Checkpoint 是否正确；
7. 模型选择和 Prompt 是否合适；
8. RAG 召回和引用是否正确；
9. Eval 是否真的测量了目标；
10. 前端是否只是没有正确展示后端状态。

### 12.4 课程结束后的进阶路线

完成核心课程后，按方向继续：

- **平台工程**：多租户、Kubernetes、推理服务、服务网格和成本平台；
- **Agent 应用**：深度研究、软件工厂、客服、销售和企业知识工作台；
- **质量工程**：Eval 数据集、Judge 校准、Trace 分析和质量门禁；
- **安全治理**：身份、数据权限、审计、红队测试和风险管理；
- **前端产品**：Agent Event、实时协作、审批体验和可解释性界面。

持续学习的核心不是追逐每天新增的框架，而是持续积累：

- 真实任务集；
- 失败样本；
- 可复现 Trace；
- 版本对比；
- 工具和权限边界；
- 用户反馈；
- 成本和质量数据。

当这些资产持续增长，你才是在构建 Agent 工程能力，而不是重复搭建 Demo。
